# MNIST Neural net in Rust

Why not?

Run `fmt.py` against the pickle file from here:
https://github.com/mnielsen/neural-networks-and-deep-learning/blob/master/data/mnist.pkl.gz

Then you can run the net
